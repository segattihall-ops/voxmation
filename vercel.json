{
  "version": 2,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @voxmation/db run generate && pnpm --filter @voxmation/api run build",
  "outputDirectory": ".",
  "framework": null
}
