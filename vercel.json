{
  "version": 2,
  "installCommand": "corepack enable && corepack prepare pnpm@10.13.1 --activate && pnpm install --no-frozen-lockfile",
  "buildCommand": "corepack enable && corepack prepare pnpm@10.13.1 --activate && pnpm --filter @voxmation/db run generate && pnpm --filter @voxmation/api run build",
  "outputDirectory": ".",
  "framework": null
}
