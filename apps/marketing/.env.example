# Environment variables for the VOXmatiON marketing app
#
# Copy this file to `.env.local` for local dev, and set the same keys in the
# Vercel project (Settings → Environment Variables). Server-side keys are not
# exposed to the browser; NEXT_PUBLIC_* keys are.

# --- ElevenLabs Conversational AI ---
# API key with Conversational AI permissions (Profile → API Keys).
ELEVENLABS_API_KEY=
# The Conversational AI agent ID (top of the agent page).
ELEVENLABS_AGENT_ID=
# The phone-number ID for the Twilio number imported into ElevenLabs
# (Conversational AI → Phone Numbers). Used by the "call me back" route to place
# outbound calls via ElevenLabs' native Twilio integration.
ELEVENLABS_AGENT_PHONE_NUMBER_ID=

# --- Demo booking (Make.com webhook) ---
# The /demo "Book Your Free Demo" form forwards each lead to a Make.com webhook,
# whose scenario emails the prospect a confirmation and notifies the team.
# Optional: defaults to the configured VOXmatiON Demo Form webhook. Override to
# point at a different Make/Zapier webhook.
DEMO_WEBHOOK_URL=
# Optional: where the team notification is delivered. Defaults to
# sales@voxmation.com.
DEMO_NOTIFICATION_EMAIL=

# --- PostHog funnel analytics (optional) ---
# Create a project at posthog.com, then copy the Project API Key here. When set,
# the site captures pageviews, autocaptured clicks, and conversion events
# (demo_talk_started, demo_form_submitted, demo_form_success, tap_to_call_clicked,
# etc.) so you can build funnels and watch session replays. Left blank, analytics
# is a no-op. This key is public/client-side by design (safe to expose).
NEXT_PUBLIC_POSTHOG_KEY=
# Optional: PostHog host. Defaults to https://us.i.posthog.com (use
# https://eu.i.posthog.com for the EU cloud).
NEXT_PUBLIC_POSTHOG_HOST=
