# Environment variables for the VOXmatiON marketing app
#
# Copy this file to `.env.local` for local dev, and set the same keys in the
# Vercel project (Settings → Environment Variables). All values are server-side
# only — none are exposed to the browser.

# --- ElevenLabs Conversational AI ---
# API key with Conversational AI permissions (Profile → API Keys).
ELEVENLABS_API_KEY=
# The Conversational AI agent ID (top of the agent page).
ELEVENLABS_AGENT_ID=
# The phone-number ID for the Twilio number imported into ElevenLabs
# (Conversational AI → Phone Numbers). Used by the "call me back" route to place
# outbound calls via ElevenLabs' native Twilio integration.
ELEVENLABS_AGENT_PHONE_NUMBER_ID=

# --- Demo booking emails (Resend) ---
# API key from https://resend.com (Dashboard → API Keys). Powers the /demo
# "Book Your Free Demo" form: it emails the lead to the sales inbox and sends
# the prospect a confirmation. This is the ONLY required variable.
RESEND_API_KEY=
# Optional: where demo-request notifications are delivered.
# Defaults to admin@voxmation.com if unset.
DEMO_NOTIFICATION_EMAIL=
# Optional: the "from" address for outgoing emails. Must be on a Resend-verified
# domain (voxmation.com) to deliver to arbitrary recipients. Defaults to
# admin@voxmation.com.
DEMO_FROM_EMAIL="VOXmatiON <admin@voxmation.com>"
